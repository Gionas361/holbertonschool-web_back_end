#!/usr/bin/env python3
"""
Takes 2 string variables and return
a string variable containing both of them.
"""


def concat(str1: str, str2: str) -> str:
    """Returns the string adition of the 2 string variables."""
    return (str1 + str2)
